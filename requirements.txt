# Core data stack
numpy>=1.26
pandas>=2.1
scipy>=1.11
scikit-learn>=1.4

# Models
lightgbm>=4.3
xgboost>=2.0

# Monitoring / experiment tracking / reporting
# Evidently >= 0.7 ships the `evidently.legacy` facade that this project uses
# for local Reports + HTML output (the new 0.7 SDK is cloud/dataset-oriented).
evidently>=0.7,<1
mlflow>=2.12
plotly>=5.20

# App / misc
streamlit>=1.36
joblib>=1.3
matplotlib>=3.8
